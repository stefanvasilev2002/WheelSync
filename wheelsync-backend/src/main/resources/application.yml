spring:
  profiles:
    active: dev   # override with SPRING_PROFILES_ACTIVE=prod in production

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  error:
    include-message: always
    include-binding-errors: always

# VAPID keys for Web Push notifications.
# Generate new keys with: npx web-push generate-vapid-keys
# Keys must be supplied via environment variables — no hardcoded defaults.
vapid:
  public-key: ${VAPID_PUBLIC_KEY}
  private-key: ${VAPID_PRIVATE_KEY}
  subject: ${VAPID_SUBJECT:mailto:admin@wheelsync.mk}
